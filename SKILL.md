---
name: experiment-performance-analysis
description: Use for any technical experiment, reproduction, training/inference performance analysis, profiling or bottleneck investigation, and experiment/performance report—even when the user does not name this skill. Trigger on “实验”, “复现”, “性能”, “训练/推理”, “吞吐”, “延迟”, “profile”, or “瓶颈”; do not use for non-technical administrative, business, or financial reports.
---

# 实验复现与性能分析

将请求落实为可复现、可比较、结论边界明确的实验与报告。适用于训练、推理、算子、分布式通信和端到端性能分析。

## 触发范围

用户只要提到技术实验、复现、性能/吞吐/延迟、profile、瓶颈，或要求实验/性能/技术报告，即使未点名本 Skill，也进入本工作流。普通行政、商务或财务报告，以及与技术实验无关的“性能”评价，不触发。

## 工作原则

- 先定义问题、成功标准和比较口径，再执行或解读实验。记录软件/硬件身份、代码版本、镜像/依赖、输入与数据边界、精度、并行策略和关键参数。
- 区分已验证、进行中、失败或未验证的结果；不把短测、首步、错误路径、未完成长稳任务或不同计算图的结果写成正式结论。
- 控制变量：一次实验只改变足以验证假设的最小变量集合。若基线与候选方案无法完全同口径，明确剩余变量，称为“训练栈/路径比较”，不要过度归因。
- 性能归因优先使用统一 FLOPs/Token 口径、迭代时间或吞吐；框架各自日志的 TFLOPS 公式不一致时，不直接用其差值分配原因。
- 任何性能优化都要有相应正确性门禁：先做最小语义或数值 A/B，再做真实输入、完整训练/采样或长稳验证。只在通过的路径上报告性能。

## 路由

1. 先确定任务属于：复现、性能基线、性能回归、瓶颈归因、通信排障、算子后端问题，或正式报告更新。一个任务可包含多个类别。
2. 建立实验台账：假设、对照/变量、命令或配置、运行状态、原始日志/trace、指标、结论和下一步。保护既有产物；未经明确授权不覆盖原配置、结果或客户资产。
3. 复现时从最小可运行路径开始，逐层扩大到目标规模。逐步记录环境、固定代码/容器身份、输入、随机性、并行拓扑和成功判据。复现失败也应形成可定位的排查记录。
4. 性能分析先采集稳态端到端基线，并分开首步、模型加载/编译、热态单步和完整 E2E。优先 Profile，而不是根据名称猜测瓶颈。
5. 每项优化按“假设 → 最小验证 → 正确性门禁 → 稳态性能 → 结论边界”闭环。无收益、退化和不可用方案也写入结果表，防止重复探索。

## 实验清单与指标口径

对多轮对照、正式报告、性能回归或需要跨环境复现的实验，先维护一份轻量实验清单，再据此写报告、总表和结论。小型一次性排障无需为了填表而延迟定位。清单格式、字段语义及训练/视频推理示例见 [实验清单与指标口径](references/experiment-manifest.md)。

- 记录一次实验的身份、输入边界、配置、测量窗口、指标、验证状态和产物位置；未知字段标记为未知，不虚构。
- 每个指标必须表达值、单位、统计方式、分母和范围；端到端时间要明确包含或排除模型加载、编译、数据预处理、I/O 与保存。
- 报告表格从清单引用共同条件和已验证指标。不同模型、精度、并行、计算量或测量范围的结果分开呈现，不隐含可比性。
- 清单存放配置、日志、trace 和大文件的路径与可选哈希，不复制大体积产物进 Git 仓库。

## 瓶颈排查

### 先 Profile

- 优先取代表性稳态窗口的 CPU/runtime 与设备 trace，必要时对基线和候选路径使用同一采样窗口与指标口径。
- 报告关键路径或 union 时间、调用次数、占比和通信—计算重叠；不要把异步 annotation/API 的累计时间直接等同网络传输时间，也不要把存在重叠的类别简单相加。
- 将优化优先级指向占关键路径最大、证据最强且可操作的项目。低占比算子不能被当作主要端到端差距的解释。

### 通信是首要瓶颈时

1. 用最小化测试隔离 collective：固定张量大小、dtype、group size、算法与 stream，测量带宽、延迟、同步/异步行为及稳定性。
2. 核实实际拓扑与数据路径：rank 映射、节点/GPU 拓扑、机内或跨机路径、NVLink/NVSwitch 是否可用及是否实际走到；同时排除输入分布、动态 shape、负载不均衡或数据等待造成的伪“通信瓶颈”。
3. 将 collective 的次数、字节数和暴露等待与训练结构对应起来（例如层数、micro batch、重建/reshard 策略）。评估减少次数/数据量、调整分片粒度、预取、重叠或以显存换通信的方案。
4. 对每种通信改动覆盖最长样本、显存峰值、正确性和长稳；只在关键路径缩短且无副作用时宣布收益。

### 算子或后端是首要瓶颈时

1. 从 profile 和调用栈确认实际 kernel/后端，而不是仅根据 Python API 名称推断。确认 shape、dtype、布局、融合与 fallback 路径。
2. 定位实现所属后端（框架原生、厂商库、编译器、第三方扩展或自定义 kernel），检查适配条件、已知限制和版本兼容性。
3. 对候选替代后端或融合实现先做真实 shape 的 microbenchmark 与数值/语义 A/B；随后通过端到端真实输入的正确性门禁。
4. 只有端到端关键路径收益、正确性和稳定性均成立，才保留替代实现；否则记录失败原因和回退路径。

## 报告输出

默认使用下列结构；按任务裁剪，但不要省略结论依据、实验条件和边界。

1. **目标与结论摘要**：任务、环境规模、当前结论、量化收益/差距、最高优先级。
2. **环境与可复现性**：硬件、驱动/软件/镜像版本、固定代码版本、输入用例、精度、并行和测量定义。
3. **性能/实验总表**：共同条件、每个方案、状态、关键指标与统计窗口。不同负载或口径分别列示。
4. **实验与排查记录**：按假设展示控制变量、命令/配置、观测、证据、结论与失败结果。
5. **性能差异归因**：profile 证据、关键路径时间、调用次数、重叠、对照实验和可信度；明确不能直接相加或尚未控制的因素。
6. **已实施优化与当前推荐配置**：只列通过正确性和稳定性门禁的路径，并陈述适用条件和资源代价。
7. **结论边界与下一步**：分别列“可以确认”和“尚不能确认”；按预期影响与验证成本排序后续实验。
8. **更新记录**：日期、版本、已完成验证、结论变化和仍在运行的实验状态。

撰写时使用精确单位、分母和统计窗口（如 step 2–50 的均值、P50/P95、加权吞吐）。清楚区分模型/框架性能与数据解码、预处理、I/O、加载或编译等端到端开销。详情及可复制表格见 [报告模板与证据标准](references/report-template.md)。

对正式报告采用双层交付：前半部分是高信息密度的决策摘要，回答结论、建议、证据和风险；后半部分是接手执行层，给未参与实验的同事或智能体提供复现、观察、继续或安全停止工作的最小事实集。完成报告后执行 [报告连续性审计](references/report-continuity.md)：将缺失的复现材料、运行状态、证据、授权或决策明确列为可回答问题，不以笼统的信息不足收尾。

## 原生飞书报告与实验附件

当用户要求创建、更新或读取飞书 Docx/Wiki 报告，或提供飞书文档 URL/token 时，优先使用 `lark-cli` 的文档能力，不用浏览器自动化代替语义读写。开始此类任务前，读取 [飞书报告与媒体交付](references/lark-report-delivery.md)，并遵守其中的格式、附件与写后核验流程。

- 读取时按需要使用 `docs +fetch` 的 outline、section、range 或 keyword 范围；编辑已有文档前获取完整结构和 block ID。对多次局部更新，避免复用已失效的 block ID。
- 创建或整段写入默认使用原生 XML；仅当用户提供本地 Markdown 或明确要求 Markdown 导入时使用 Markdown。用段落承载分析叙述、用表格展示行列指标；高亮块、分栏、颜色和画板只在确有助益时使用。
- 文档写入、上传附件或更改云端内容前，确认目标文档、拟写内容和附件范围已获用户授权；写入后重新读取相关范围，确认正文、附件与链接实际存在。
- 对视频或大型实验产物，交付“关键帧/缩略图预览 + 具名原始附件 + 复现实验元数据”。大体积视频、trace、日志和模型权重只保存在约定的产物存储位置，不提交到本 Skill 仓库。

## 安全与交付

- 对会修改生产训练、容器、权限、配置或外部资产的操作，先说明影响并在执行前取得所需授权。
- 保留命令、配置 diff、日志/trace 和原始结果位置，使结论可审计与复现。
- 若缺少 profile、原始日志、配置或正确性证据，输出待验证结论和最小下一步，而不是以推测替代证据。

## 真实反馈驱动的维护

当实际任务暴露出 Skill 的缺口或误导性行为时，先记录触发请求、可观察的问题、期望行为和适用边界，再决定是否修改规则。可泛化的修正应关联 Issue、增加或更新一个验收场景，并在 PR 中说明其证据与副作用；一次性的项目偏好、偶发环境故障或尚无证据的猜测不应直接固化为通用要求。
